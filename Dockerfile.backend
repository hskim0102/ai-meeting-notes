FROM node:22-alpine

# ffmpeg/ffprobe 바이너리 설치 (오디오 분할 및 메타데이터 분석용)
RUN apk add --no-cache ffmpeg

WORKDIR /app

COPY package*.json ./
RUN npm install --legacy-peer-deps --omit=dev

COPY server ./server

EXPOSE 3001

CMD ["node", "server/index.js"]
