# AI 스마트 회의록 Agent — 제품 요구사항 정의서 (PRD)

> **과제명**: AI 스마트 회의록 Agent
> **과제 유형**: 사내 AX(AI Transformation) 혁신 과제
> **문서 버전**: v1.1
> **최종 수정일**: 2026-03-22
> **작성자**: AX 혁신 TF

---

## 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [기술 스택](#2-기술-스택)
3. [시스템 아키텍처](#3-시스템-아키텍처)
4. [핵심 기능 명세](#4-핵심-기능-명세)
5. [화면 구성 및 UI/UX 설계](#5-화면-구성-및-uiux-설계)
6. [API 명세](#6-api-명세)
7. [비기능 요구사항](#7-비기능-요구사항)
8. [향후 계획 (Milestone)](#8-향후-계획-milestone)

---

## 1. 프로젝트 개요

### 1.1 배경 및 필요성

사내 임직원들은 일 평균 2~3회의 회의에 참석하며, 회의 후 수기로 회의록을 작성하고 액션 아이템을 추적하는 데 상당한 시간을 소모하고 있다. 기존 회의록 작성 프로세스는 다음과 같은 문제를 안고 있다:

- **생산성 저하**: 회의 내용 정리에 회의 시간의 30~50% 추가 소요
- **정보 누락**: 수기 작성 시 핵심 논의사항 및 결정사항 누락 빈번
- **추적 불가**: 액션 아이템의 체계적 관리 부재로 후속 조치 지연
- **검색 한계**: 과거 회의 내용의 검색 및 재활용이 사실상 불가능

### 1.2 프로젝트 목표

AI 기반의 스마트 회의록 Agent를 도입하여 **회의 녹음 → 텍스트 변환 → AI 요약 → 액션 아이템 자동 추출**의 End-to-End 자동화 파이프라인을 구축한다.

| 목표 | 기대 효과 |
|------|-----------|
| 회의록 작성 자동화 | 회의록 작성 시간 80% 이상 절감 |
| 액션 아이템 자동 추출 | 후속 조치 누락률 감소, 업무 추적성 확보 |
| 회의 검색 및 아카이빙 | 과거 회의 내용 즉시 접근 및 활용 가능 |
| 화자 구분 및 발언 추적 | 발언 책임 명확화, 맥락 이해도 향상 |

### 1.3 대상 사용자

- **1차 사용자**: 사내 전 임직원 (회의 참석자)
- **2차 사용자**: 팀 리더 / PM (액션 아이템 관리 및 추적)
- **관리자**: IT 운영팀 (시스템 관리 및 모니터링)

### 1.4 제품 범위

| 포함 | 미포함 (향후 고려) |
|------|---------------------|
| 실시간 마이크 녹음 | 화상회의 플랫폼 직접 연동 (Zoom, Teams) |
| 오디오 파일 업로드 | 다국어 동시통역 |
| STT 텍스트 변환 | 사내 메신저 자동 알림 |
| AI 요약 및 액션 아이템 추출 | 외부 캘린더 동기화 |
| 대시보드 및 회의 관리 | 모바일 네이티브 앱 |
| 회의실 예약 | — |
| 회의 검색 | — |
| 화자 구분 | — |

---

## 2. 기술 스택

### 2.1 프론트엔드

| 기술 | 버전 | 용도 |
|------|------|------|
| **Vue.js 3** | ^3.5 | SPA 프레임워크 (Composition API, `<script setup>`) |
| **Vue Router** | ^4.6 | 클라이언트 사이드 라우팅 |
| **Tailwind CSS** | v4 | 유틸리티 기반 스타일링 |
| **Vite** | ^8.0 | 빌드 도구 및 개발 서버 |

### 2.2 백엔드

| 기술 | 버전 | 용도 |
|------|------|------|
| **Node.js** | 20 LTS+ | 런타임 환경 |
| **Express.js** | ^5.2 | REST API 서버 |
| **Multer** | ^2.1 | 멀티파트 파일 업로드 처리 |
| **fluent-ffmpeg** | ^2.1 | 오디오 파일 분할 (Chunking) |

### 2.3 AI 연동

| 기술 | 용도 |
|------|------|
| **OpenAI Whisper API** | 음성-텍스트 변환 (STT) |
| **Dify Workflow API** | 회의록 요약, 액션 아이템 추출, 키워드 JSON 생성 |

### 2.4 인프라 (현재 → 향후)

| 구분 | 현재 (프로토타입) | 향후 (운영) |
|------|-------------------|-------------|
| 호스팅 | Localhost (dev) | 프라이빗 클라우드 / 사내망 |
| 스토리지 | 로컬 파일시스템 | 오브젝트 스토리지 (S3 호환) |
| DB | 인메모리 / Mock | PostgreSQL 또는 MongoDB |
| 인증 | 없음 | SSO / LDAP 연동 |

### 2.5 모듈 구성 (ESM)

프로젝트 전체는 `"type": "module"` 기반의 ESM(ECMAScript Modules)으로 구성되며, CommonJS 모듈은 사용하지 않는다.

---

## 3. 시스템 아키텍처

### 3.1 전체 시스템 구성도

```
┌─────────────────────────────────────────────────────────┐
│                    Frontend (Vue.js 3)                   │
│                    http://localhost:3000                  │
│                                                         │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌────────┐│
│  │Dashboard│ │ Meeting │ │  New    │ │ Room    │ │ Search ││
│  │  View   │ │  Detail │ │ Meeting │ │ Booking │ │  View  ││
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘ └────────┘│
│                       │                                  │
│              ┌────────┴────────┐                        │
│              │   API Client    │                        │
│              │  (api.js)       │                        │
│              └────────┬────────┘                        │
└───────────────────────┼─────────────────────────────────┘
                        │ HTTP (REST)
┌───────────────────────┼─────────────────────────────────┐
│              Backend (Express.js 5)                      │
│              http://localhost:3001                        │
│                       │                                  │
│    ┌──────────────────┼──────────────────┐              │
│    │                  │                  │              │
│  ┌──┴───────┐ ┌─────┴─────┐ ┌─────┴────┐ ┌──────────┐│
│  │/api/     │ │/api/      │ │/api/     │ │/api/     ││
│  │transcribe│ │summarize  │ │meetings  │ │rooms     ││
│  │          │ │           │ │search    │ │reserv.   ││
│  └─┬────────┘ └─────┬─────┘ └──────────┘ └──────────┘│
│    │                  │                                │
│  ┌─┴──────────┐  ┌───┴──────────┐                    │
│  │audioSplitter│  │difyService   │                    │
│  │whisperSvc   │  │              │                    │
│  └─┬──────────┘  └───┬──────────┘                    │
└────┼──────────────────┼──────────────────────────────┘
     │                  │
     ▼                  ▼
┌──────────┐     ┌──────────┐
│ OpenAI   │     │  Dify    │
│ Whisper  │     │ Workflow │
│ API      │     │ API      │
└──────────┘     └──────────┘
```

### 3.2 데이터 흐름

```
[사용자]
   │
   ├─ 마이크 녹음 (WebM) ──┐
   │                        ├──→ [오디오 파일]
   └─ 파일 업로드 ─────────┘        │
                                     ▼
                              [파일 크기 확인]
                                     │
                        ┌────────────┼────────────┐
                        │            │            │
                    ≤ 25MB      > 25MB        에러
                        │            │
                        │     [ffmpeg 분할]
                        │     (20MB chunks)
                        │            │
                        ▼            ▼
                    [Whisper API STT 호출]
                    (chunk별 병렬 처리)
                              │
                              ▼
                    [텍스트 병합 (Transcript)]
                              │
                              ▼
                    [Dify Workflow API 호출]
                              │
                              ▼
                    ┌─────────┼─────────┐
                    │         │         │
                 [TL;DR]  [액션    [키워드
                          아이템]   JSON]
                    │         │         │
                    └─────────┼─────────┘
                              ▼
                    [결과 화면 렌더링]
```

---

## 4. 핵심 기능 명세

### 4.1 실시간 마이크 녹음

| 항목 | 상세 |
|------|------|
| **기능 ID** | F-REC-001 |
| **기능명** | 브라우저 실시간 마이크 녹음 |
| **설명** | 사용자가 브라우저에서 직접 마이크를 통해 회의 내용을 실시간 녹음한다 |
| **기술 구현** | MediaRecorder API (WebM/Opus 포맷) |
| **컴포넌트** | `LiveRecorder.vue` |

**상세 요구사항**:

- 녹음 시작/일시정지/재개/종료 제어 UI 제공
- 실시간 녹음 시간 타이머 표시 (HH:MM:SS)
- 오디오 파형(waveform) 시각화를 통한 녹음 상태 피드백
- 녹음 완료 시 WebM 파일로 자동 변환
- 브라우저 마이크 권한 요청 및 권한 거부 시 안내 메시지 표시
- 녹음 중 페이지 이탈 시 경고(beforeunload) 처리

### 4.2 오디오 파일 업로드

| 항목 | 상세 |
|------|------|
| **기능 ID** | F-UPL-001 |
| **기능명** | 드래그 앤 드롭 오디오 파일 업로드 |
| **설명** | 기존 녹음 파일(MP3, WAV, M4A, WebM 등)을 업로드하여 텍스트 변환을 수행한다 |
| **기술 구현** | Multer (서버), Drag & Drop API (클라이언트) |
| **컴포넌트** | `AudioUploader.vue` |

**상세 요구사항**:

- 지원 포맷: MP3, WAV, M4A, WebM, OGG, FLAC
- 드래그 앤 드롭 영역 및 파일 선택 버튼 제공
- 업로드 진행률 프로그레스 바 표시
- 파일 유효성 검증 (포맷, 크기 제한)
- 최대 파일 크기: 500MB (분할 처리 포함)

### 4.3 대용량 파일 Chunking 및 STT 변환

| 항목 | 상세 |
|------|------|
| **기능 ID** | F-STT-001 |
| **기능명** | 오디오 분할 및 STT 텍스트 변환 |
| **설명** | 25MB 이상의 대용량 오디오 파일을 자동 분할하고 Whisper API를 통해 텍스트로 변환한다 |
| **기술 구현** | fluent-ffmpeg (분할), OpenAI Whisper API (STT) |
| **서비스** | `audioSplitter.js`, `whisperService.js` |

**상세 요구사항**:

- **분할 기준**: 25MB 초과 시 20MB 단위로 자동 분할
- **분할 방식**: ffmpeg를 이용한 시간 기반 분할 (오디오 품질 유지)
- **병렬 처리**: 분할된 청크를 순차/병렬로 Whisper API에 전송
- **텍스트 병합**: 각 청크의 STT 결과를 시간순으로 정렬 및 병합
- **임시 파일 관리**: 업로드 파일 및 분할 청크는 처리 완료 후 자동 삭제 (`uploads/`, `temp/`)
- **에러 처리**: 개별 청크 실패 시 재시도 로직 (최대 2회)
- **타임스탬프 보존**: 각 청크별 시작 시간 오프셋을 반영하여 전체 타임라인 유지

**처리 흐름**:

```
오디오 파일 (원본)
    │
    ├─ ≤ 25MB → Whisper API 직접 호출
    │
    └─ > 25MB → ffmpeg 분할 (20MB chunks)
                    │
                    ├─ chunk_001.webm → Whisper API → 텍스트 1
                    ├─ chunk_002.webm → Whisper API → 텍스트 2
                    └─ chunk_00N.webm → Whisper API → 텍스트 N
                                                        │
                                                   [시간순 병합]
                                                        │
                                                   완성된 Transcript
```

### 4.4 Dify API 연동 — AI 구조화 요약

| 항목 | 상세 |
|------|------|
| **기능 ID** | F-SUM-001 |
| **기능명** | AI 기반 회의록 구조화 요약 |
| **설명** | STT 결과 텍스트를 Dify Workflow API에 전달하여 구조화된 회의록 결과를 생성한다 |
| **기술 구현** | Dify Workflow API (HTTP POST) |
| **서비스** | `difyService.js` |

**Dify 출력 구조**:

```json
{
  "summary": "회의 핵심 내용 요약 (TL;DR)",
  "action_items": [
    {
      "id": "uuid",
      "task": "액션 아이템 내용",
      "assignee": "담당자",
      "due_date": "마감일 (추정)",
      "priority": "high | medium | low",
      "status": "pending"
    }
  ],
  "keywords": ["키워드1", "키워드2", "키워드3"],
  "key_decisions": [
    {
      "decision": "결정 사항",
      "context": "결정 배경"
    }
  ],
  "participants": ["참석자1", "참석자2"]
}
```

**상세 요구사항**:

- Dify API Key는 서버 사이드에서만 관리 (클라이언트 노출 금지)
- API 타임아웃: 120초 (LLM 처리 지연 대비)
- 응답 파서: JSON-string 형태와 개별 필드 형태 모두 처리 가능
- 요약 실패 시 원본 텍스트 보존 및 재시도 옵션 제공

### 4.5 대시보드 및 회의 관리

| 항목 | 상세 |
|------|------|
| **기능 ID** | F-DSH-001 |
| **기능명** | 메인 대시보드 |
| **설명** | 회의 현황 통계, 최근 회의 목록, 액션 아이템 진행 상태를 한눈에 파악할 수 있는 대시보드 |
| **컴포넌트** | `DashboardView.vue`, `StatCard.vue`, `MeetingCard.vue` |

**대시보드 표시 항목**:

| 통계 카드 | 내용 |
|-----------|------|
| 전체 회의 수 | 등록된 총 회의 건수 |
| 이번 주 회의 | 금주 진행된 회의 건수 |
| 미완료 액션 아이템 | 상태가 `pending`인 액션 아이템 수 |
| 완료율 | 전체 대비 완료된 액션 아이템 비율 (%) |

**필터링 기능**:

- 기간별 필터 (오늘, 이번 주, 이번 달, 사용자 지정)
- 상태별 필터 (진행 중, 완료, 보관)
- 키워드 검색
- 참석자별 필터

### 4.6 스플릿 뷰 — 회의 상세 화면

| 항목 | 상세 |
|------|------|
| **기능 ID** | F-DET-001 |
| **기능명** | 회의 상세 스플릿 뷰 |
| **설명** | 오디오 재생기와 스크립트를 양쪽으로 분할 표시하여 동기화된 회의 내용 확인 및 편집이 가능한 상세 화면 |
| **컴포넌트** | `MeetingDetailView.vue` |

**상세 요구사항**:

- **좌측 패널**: 오디오 플레이어 + AI 요약 결과 (TL;DR, 액션 아이템, 키워드)
- **우측 패널**: 전체 Transcript (타임스탬프 포함)
- **오디오-스크립트 동기화**: 오디오 재생 위치에 따라 해당 텍스트 구간 자동 하이라이트
- **인라인 텍스트 수정**: 사용자가 Transcript 내 오류를 직접 클릭하여 수정 가능
- **탭 구성**: 요약 / 액션 아이템 / 전문(Transcript) 탭 전환
- **내보내기**: 회의록을 PDF, Markdown, 클립보드 복사로 내보내기

### 4.7 회의실 예약 기능

| 항목 | 상세 |
|------|------|
| **기능 ID** | F-RSV-001 |
| **기능명** | 회의실 예약 및 관리 |
| **우선순위** | Medium |
| **설명** | 사내 회의실의 예약 현황 조회 및 예약/수정/취소 기능을 제공하며, 회의록과 자동 연동한다 |
| **컴포넌트** | `RoomListView.vue`, `RoomCalendarView.vue`, `ReservationModal.vue` |

#### 4.7.1 사용자 스토리

| ID | 역할 | 스토리 | 인수 조건 |
|----|------|--------|-----------|
| US-RSV-01 | 임직원 | 특정 날짜/시간에 사용 가능한 회의실을 조회하고 싶다 | 날짜+시간 선택 시 가용 회의실만 필터링 표시 |
| US-RSV-02 | 임직원 | 회의실을 예약하고 참석자를 초대하고 싶다 | 예약 완료 시 참석자 목록에 반영 |
| US-RSV-03 | 임직원 | 내가 만든 예약을 수정하거나 취소하고 싶다 | 본인 예약만 수정/취소 가능, 과거 예약은 수정 불가 |
| US-RSV-04 | 임직원 | 정기 회의를 반복 예약하고 싶다 | 주간/격주/월간 반복 패턴 지원 |
| US-RSV-05 | 임직원 | 회의 시작 전 알림을 받고 싶다 | 10분/30분 전 브라우저 알림 |
| US-RSV-06 | 관리자 | 회의실 정보를 등록/수정/삭제하고 싶다 | CRUD 관리 화면 제공 |

#### 4.7.2 상세 요구사항

**회의실 관리**:

- **회의실 정보 표시**: 이름, 위치(건물/층), 수용 인원, 보유 장비 (프로젝터, 화이트보드, 화상회의 장비, 마이크 등)
- **회의실 상태**: 사용 가능 / 사용 중 / 점검 중 실시간 상태 표시
- **회의실 이미지**: 회의실 사진 등록 (선택사항)

**예약 생성**:

- 날짜, 시작 시간, 종료 시간, 회의실, 회의 주제, 참석자 입력
- 최소 예약 단위: 30분, 최대 예약 시간: 4시간
- **반복 예약**: 매주 / 격주 / 매월 (지정 요일) 반복 생성 지원
- 예약 시 회의실 가용 여부 실시간 검증

**예약 수정/취소**:

- 본인이 생성한 예약에 한해 수정 및 취소 가능
- 과거 시점의 예약은 수정/취소 불가 (읽기 전용)
- 반복 예약 수정 시: "이 일정만" / "이후 모든 일정" 선택 옵션

**충돌 방지**:

- 동일 회의실 + 동일 시간대 중복 예약 차단 (서버 사이드 검증)
- 예약 폼에서 실시간 충돌 여부 표시 (클라이언트 사이드 프리체크)
- 동시 예약 요청 시 선착순 처리 (Optimistic Locking)

**회의록 연동**:

- 예약된 회의 시간에 생성된 회의록과 자동 매칭 (시간대 기반)
- 수동 연결: 기존 회의록을 예약 건에 직접 연결 가능
- 회의 상세 화면에서 연결된 예약 정보 표시

**알림**:

- 회의 시작 10분/30분 전 브라우저 Push 알림 (Notification API)
- 예약 확인/변경/취소 시 참석자 알림

#### 4.7.3 화면 설계

**달력 뷰 (주간/일간)**:

```
┌──────────────────────────────────────────────────┐
│  ◀ 2026년 3월 4주차 ▶         [일간] [주간] [월간] │
├─────────┬──────┬──────┬──────┬──────┬──────┬──────┤
│ 시간    │ 월   │ 화   │ 수   │ 목   │ 금   │      │
├─────────┼──────┼──────┼──────┼──────┼──────┤      │
│ 09:00   │      │██████│      │      │      │      │
│ 09:30   │      │██ A ██│      │      │      │ 회의실│
│ 10:00   │██████│██████│      │██████│      │ 필터  │
│ 10:30   │██ B ██│      │      │██ C ██│      │      │
│ 11:00   │██████│      │██████│██████│      │ [전체]│
│ 11:30   │      │      │██ D ██│      │      │ [대] │
│  ...    │      │      │██████│      │      │ [소] │
└─────────┴──────┴──────┴──────┴──────┴──────┴──────┘
                                    [+ 새 예약]
```

**예약 모달**:

```
┌─────────────────────────────────────┐
│         회의실 예약                    │
├─────────────────────────────────────┤
│ 회의 주제: [Q1 사업 검토 회의      ] │
│ 회의실:    [대회의실 A        ▼]     │
│ 날짜:      [2026-03-25   📅]        │
│ 시작:      [14:00  ▼]               │
│ 종료:      [15:30  ▼]               │
│ 참석자:    [홍길동, 김철수  + 추가]  │
│ 반복:      [반복 없음          ▼]    │
│                                     │
│ ✅ 해당 시간 예약 가능               │
│                                     │
│         [취소]    [예약하기]          │
└─────────────────────────────────────┘
```

#### 4.7.4 데이터 모델

**회의실 (Room)**:

```json
{
  "id": "room-01",
  "name": "대회의실 A",
  "location": {
    "building": "본관",
    "floor": "3층",
    "detail": "엘리베이터 옆"
  },
  "capacity": 20,
  "equipment": ["프로젝터", "화이트보드", "화상회의", "마이크"],
  "image_url": "/rooms/room-01.jpg",
  "status": "available",
  "created_at": "2026-01-01T00:00:00Z"
}
```

**예약 (Reservation)**:

```json
{
  "reservation_id": "uuid",
  "room_id": "room-01",
  "title": "Q1 사업 검토 회의",
  "date": "2026-03-22",
  "start_time": "14:00",
  "end_time": "15:30",
  "organizer": {
    "id": "user-001",
    "name": "홍길동",
    "department": "기획팀"
  },
  "participants": [
    { "id": "user-002", "name": "김철수" },
    { "id": "user-003", "name": "이영희" }
  ],
  "recurrence": {
    "type": "weekly",
    "interval": 1,
    "end_date": "2026-06-30",
    "days_of_week": ["MON"]
  },
  "linked_meeting_id": "meeting-uuid",
  "notification": {
    "before_minutes": [10, 30]
  },
  "status": "confirmed",
  "created_at": "2026-03-20T09:00:00Z",
  "updated_at": "2026-03-20T09:00:00Z"
}
```

### 4.8 화자 구분 (Speaker Diarization)

| 항목 | 상세 |
|------|------|
| **기능 ID** | F-SPK-001 |
| **기능명** | 회의 내용 화자 구분 |
| **설명** | 회의 녹음에서 발언자를 자동으로 구분하여 누가 어떤 발언을 했는지 식별한다 |

**상세 요구사항**:

- **자동 화자 분리**: 오디오에서 발언자 변경 구간을 자동 감지
- **화자 라벨링**: 초기에는 "화자 1", "화자 2" 등으로 자동 라벨링
- **수동 매핑**: 사용자가 화자 라벨을 실제 참석자 이름으로 매핑 가능
- **Transcript 반영**: 화자 정보가 포함된 Transcript 출력

**출력 형태 (예시)**:

```
[화자 1 - 홍길동] 14:00:12
이번 분기 매출 목표 달성률이 92%로 집계되었습니다.

[화자 2 - 김철수] 14:00:35
해외 사업부 실적이 예상보다 높았는데, 구체적인 수치는?

[화자 1 - 홍길동] 14:00:48
해외 부문은 전년 대비 15% 성장하여...
```

**기술 구현 방안**:

| 방안 | 설명 | 장단점 |
|------|------|--------|
| OpenAI Whisper + 후처리 | Whisper 타임스탬프 기반 화자 분리 | 구현 간단 / 정확도 제한적 |
| pyannote.audio | 오픈소스 화자 분리 라이브러리 | 정확도 우수 / 별도 Python 서비스 필요 |
| 클라우드 Diarization API | Google / Azure Speech Service | 높은 정확도 / 외부 의존성 |

### 4.9 회의 검색 기능

| 항목 | 상세 |
|------|------|
| **기능 ID** | F-SRC-001 |
| **기능명** | 회의 통합 검색 |
| **우선순위** | High |
| **설명** | 회의 제목, 참석자, 키워드, 본문 내용 등 다양한 기준으로 과거 회의를 빠르게 검색한다 |
| **컴포넌트** | `SearchView.vue`, `SearchBar.vue`, `SearchResultCard.vue` |

#### 4.9.1 사용자 스토리

| ID | 역할 | 스토리 | 인수 조건 |
|----|------|--------|-----------|
| US-SRC-01 | 임직원 | 특정 키워드로 과거 회의를 찾고 싶다 | 키워드 입력 시 관련 회의 목록 표시, 매칭 구간 하이라이트 |
| US-SRC-02 | 임직원 | 특정 기간의 회의만 필터링하고 싶다 | 날짜 범위 선택 시 해당 기간 회의만 표시 |
| US-SRC-03 | 임직원 | 특정 사람이 참석한 회의를 모아보고 싶다 | 참석자 이름으로 필터링 가능 |
| US-SRC-04 | PM | 특정 프로젝트 관련 회의를 한번에 조회하고 싶다 | 키워드 태그 조합 검색으로 프로젝트별 회의 그룹핑 |
| US-SRC-05 | 임직원 | 검색 결과에서 바로 해당 발언 위치로 이동하고 싶다 | 검색 결과 클릭 시 Transcript 해당 위치로 스크롤 |
| US-SRC-06 | 임직원 | 자주 하는 검색을 저장해두고 싶다 | 검색 조건 저장/불러오기 기능 |

#### 4.9.2 상세 요구사항

**빠른 검색 (Global Search Bar)**:

- 헤더 영역에 항상 노출되는 글로벌 검색 바 (`Ctrl/Cmd + K` 단축키)
- 검색어 입력 시 실시간 자동 완성 (회의 제목, 키워드, 참석자 이름 제안)
- 최근 검색어 히스토리 표시 (최대 10건)
- 디바운스 적용 (300ms) — 타이핑 완료 후 API 호출

**고급 검색 (Advanced Search)**:

- **전문 검색(Full-Text Search)**: 회의 Transcript 본문, 요약, 액션 아이템 내 키워드 검색
- **필터 조합**: 아래 필터를 AND 조건으로 조합 가능
  - 날짜 범위 (시작일 ~ 종료일)
  - 참석자 (다중 선택)
  - 키워드 태그 (다중 선택)
  - 화자 (특정 발언자의 발언만 검색)
  - 회의 상태 (진행 중 / 완료 / 보관)
- **검색 연산자 지원**: `"정확한 구문"`, `+필수단어`, `-제외단어`, `OR` 조합

**검색 결과 표시**:

- **스니펫 하이라이팅**: 검색어 매칭 구간 전후 맥락 텍스트와 함께 하이라이트 표시
- **관련도 점수**: 검색 결과별 관련도 표시 (매칭 횟수, 위치 가중치 기반)
- **정렬 옵션**: 관련도순 (기본), 최신순, 오래된순
- **페이지네이션**: 20건 단위 무한 스크롤 또는 페이지 전환
- **결과 내 재검색**: 검색 결과 범위 내에서 추가 키워드로 좁히기

**검색 → 상세 연동**:

- 검색 결과 클릭 시 해당 회의 상세 페이지로 이동
- Transcript 내 검색어 매칭 위치로 자동 스크롤 및 하이라이트
- 오디오 플레이어를 해당 발언 시점으로 자동 시크(seek)

#### 4.9.3 검색 대상 필드 및 가중치

| 필드 | 검색 방식 | 관련도 가중치 |
|------|-----------|:------------:|
| 회의 제목 | 부분 일치 (LIKE) | ×3.0 |
| 키워드 태그 | 정확 일치 | ×2.5 |
| AI 요약 (TL;DR) | 전문 검색 | ×2.0 |
| 액션 아이템 내용 | 전문 검색 | ×1.5 |
| 참석자 이름 | 부분 일치 | ×1.5 |
| Transcript 본문 | 전문 검색 | ×1.0 |

#### 4.9.4 화면 설계

**글로벌 검색 바 (헤더)**:

```
┌─────────────────────────────────────────────────────┐
│  🔍 회의 검색... (Ctrl+K)                            │
│  ┌─────────────────────────────────────────────────┐ │
│  │ 최근 검색: Q1 매출 | 김철수 | 보안 점검          │ │
│  │─────────────────────────────────────────────────│ │
│  │ 📄 Q1 사업 전략 회의          2026-03-20        │ │
│  │ 📄 Q1 매출 목표 리뷰          2026-03-15        │ │
│  │ 🏷️ 키워드: Q1, 매출, 목표                       │ │
│  │ 👤 참석자: 김철수 (3건)                          │ │
│  └─────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

**검색 결과 페이지**:

```
┌────────────────────────────────────────────────────────┐
│  🔍 ["Q1 매출"]                    [고급 검색 ▼]       │
│                                                        │
│  ┌─ 필터 ────────────────────────────────────────────┐ │
│  │ 기간: [2026-01 ~ 2026-03]  참석자: [전체 ▼]      │ │
│  │ 키워드: [전체 ▼]  상태: [전체 ▼]                  │ │
│  └───────────────────────────────────────────────────┘ │
│                                                        │
│  검색 결과 12건  |  정렬: [관련도순 ▼]                  │
│                                                        │
│  ┌───────────────────────────────────────────────────┐ │
│  │ 📄 Q1 사업 검토 회의                  2026-03-20  │ │
│  │ 참석자: 홍길동, 김철수, 이영희                     │ │
│  │ "...이번 분기 【Q1 매출】 목표 달성률이            │ │
│  │  92%로 집계되었습니다. 해외 부문은..."              │ │
│  │ 🏷️ Q1  매출  해외사업                              │ │
│  └───────────────────────────────────────────────────┘ │
│  ┌───────────────────────────────────────────────────┐ │
│  │ 📄 Q1 매출 목표 수립 회의             2026-01-08  │ │
│  │ 참석자: 홍길동, 박지민                             │ │
│  │ "...【Q1 매출】 목표를 전년 대비 10%               │ │
│  │  상향 조정하는 것으로 합의..."                      │ │
│  │ 🏷️ Q1  매출  목표설정                              │ │
│  └───────────────────────────────────────────────────┘ │
│                                                        │
│  [더 보기...]                                          │
└────────────────────────────────────────────────────────┘
```

#### 4.9.5 기술 구현 단계별 전략

| 단계 | 기술 | 적용 시점 | 설명 |
|------|------|-----------|------|
| Phase 1 | JavaScript Array Filter | 프로토타입 | Mock 데이터 대상 클라이언트 필터링 |
| Phase 2 | DB 전문 검색 (tsvector / text index) | DB 도입 후 | PostgreSQL `tsvector` 또는 MongoDB Text Index |
| Phase 3 | Elasticsearch | 운영 고도화 | 대용량 데이터 대응, 형태소 분석 (Nori), 관련도 점수 |
| 향후 | 벡터 검색 (RAG) | AI 고도화 | 의미 기반 검색 — "지난번 예산 관련 논의" → 유사 회의 추천 |

---

## 5. 화면 구성 및 UI/UX 설계

### 5.1 라우팅 구조

| 경로 | 화면명 | 설명 |
|------|--------|------|
| `/` | 대시보드 | 회의 현황 통계 및 최근 회의 목록 |
| `/meetings/new` | 새 회의 | 녹음/업로드 → STT → AI 요약 |
| `/meetings` | 회의 목록 | 전체 회의 리스트 (필터, 검색) |
| `/meetings/:id` | 회의 상세 | 스플릿 뷰 (오디오+요약+Transcript) |
| `/action-items` | 액션 아이템 | 전체 액션 아이템 관리 |
| `/rooms` | 회의실 목록 | 회의실 현황 카드 뷰 (수용인원, 장비, 실시간 상태) |
| `/rooms/calendar` | 회의실 예약 달력 | 주간/일간 타임라인 뷰, 예약 생성/수정/취소 |
| `/rooms/:id` | 회의실 상세 | 개별 회의실 예약 현황 및 정보 |
| `/search` | 통합 검색 | 글로벌 검색 결과 (필터, 정렬, 스니펫 하이라이팅) |

### 5.2 레이아웃

```
┌────────────────────────────────────────────────┐
│  ┌────────┐  ┌──────────────────────────────┐  │
│  │        │  │         Header Bar            │  │
│  │        │  ├──────────────────────────────┤  │
│  │  Side  │  │                              │  │
│  │  bar   │  │                              │  │
│  │  Nav   │  │       Main Content           │  │
│  │        │  │       (router-view)          │  │
│  │        │  │                              │  │
│  │        │  │                              │  │
│  │        │  │                              │  │
│  └────────┘  └──────────────────────────────┘  │
└────────────────────────────────────────────────┘
```

### 5.3 디자인 가이드라인

- **테마**: 밝은 배경 + 포인트 컬러 (사내 CI 컬러 적용)
- **타이포그래피**: Pretendard (한글 최적화 웹폰트)
- **반응형**: 최소 1280px 이상 데스크톱 최적화 (태블릿 대응은 향후)
- **접근성**: WCAG 2.1 AA 수준 준수 목표
- **한글 UI**: 전체 인터페이스 한국어 기본

---

## 6. API 명세

### 6.1 현재 구현된 API

#### POST `/api/transcribe`

음성 파일을 업로드하여 텍스트로 변환한다.

| 항목 | 상세 |
|------|------|
| Method | POST |
| Content-Type | multipart/form-data |
| Request Body | `audio`: 오디오 파일 (필수) |
| Response | `{ "transcript": "변환된 텍스트" }` |
| 에러 코드 | 400 (잘못된 파일), 413 (용량 초과), 500 (서버 오류) |

#### POST `/api/summarize`

Transcript 텍스트를 AI로 요약한다.

| 항목 | 상세 |
|------|------|
| Method | POST |
| Content-Type | application/json |
| Request Body | `{ "transcript": "회의 전문 텍스트" }` |
| Response | `{ "summary": "...", "action_items": [...], "keywords": [...] }` |
| Timeout | 120초 |
| 에러 코드 | 400 (빈 텍스트), 504 (타임아웃), 500 (서버 오류) |

#### GET `/api/transcribe/health`, `/api/summarize/health`

서비스 헬스 체크 엔드포인트.

### 6.2 추가 필요 API (향후 구현)

| Endpoint | Method | 설명 |
|----------|--------|------|
| `/api/meetings` | GET | 회의 목록 조회 (페이지네이션, 필터) |
| `/api/meetings/:id` | GET | 회의 상세 조회 |
| `/api/meetings` | POST | 회의 생성 (메타데이터 저장) |
| `/api/meetings/:id` | PUT | 회의 수정 (Transcript 인라인 편집 등) |
| `/api/meetings/:id` | DELETE | 회의 삭제 |
| `/api/meetings/search` | GET | 회의 통합 검색 |
| `/api/action-items` | GET | 액션 아이템 목록 |
| `/api/action-items/:id` | PATCH | 액션 아이템 상태 변경 |
| `/api/rooms` | GET | 회의실 목록 조회 |
| `/api/rooms/:id` | GET | 회의실 상세 정보 |
| `/api/rooms/availability` | GET | 특정 날짜/시간 회의실 가용성 조회 (`?date=&start=&end=`) |
| `/api/reservations` | GET | 예약 목록 조회 (`?room_id=&date=&week=`) |
| `/api/reservations` | POST | 회의실 예약 생성 (반복 예약 포함) |
| `/api/reservations/:id` | PUT | 예약 수정 (`?scope=single\|future`) |
| `/api/reservations/:id` | DELETE | 예약 취소 |
| `/api/search` | GET | 통합 검색 (`?q=&from=&to=&participants=&tags=&sort=&page=`) |
| `/api/search/suggest` | GET | 검색 자동완성 제안 (`?q=`, 디바운스 대응) |

---

## 7. 비기능 요구사항

### 7.1 성능

| 항목 | 목표 |
|------|------|
| STT 처리 시간 | 오디오 1분당 10초 이내 |
| AI 요약 응답 시간 | 60초 이내 (일반), 120초 이내 (장문) |
| 페이지 로딩 시간 | 초기 로딩 3초 이내, 이후 1초 이내 |
| 파일 업로드 | 100MB 파일 기준 30초 이내 (사내망) |

### 7.2 보안

| 항목 | 요구사항 |
|------|----------|
| API Key 관리 | 서버 사이드 환경변수 (.env) 관리, 클라이언트 노출 차단 |
| 파일 보안 | 업로드 파일 처리 후 즉시 삭제, 임시 디렉토리 자동 정리 |
| 인증/인가 | 프로토타입: 없음 → 운영: SSO/LDAP 연동 |
| 데이터 암호화 | 운영 환경에서 HTTPS 필수, 저장 데이터 암호화 |
| 접근 제어 | 회의록 접근 권한: 참석자 + 공유 대상자만 |

### 7.3 가용성 및 안정성

| 항목 | 목표 |
|------|------|
| 서비스 가용률 | 99.5% (업무 시간 기준) |
| 에러 처리 | 외부 API 장애 시 graceful degradation |
| 데이터 백업 | 일 1회 자동 백업 (운영 환경) |

---

## 8. 향후 계획 (Milestone)

### Phase 1 — 프로토타입 (Localhost) ✅ 진행 중

> **목표**: 핵심 기능 동작 검증 및 사내 데모
> **환경**: 로컬 개발 환경 (localhost:3000 / :3001)

| 항목 | 상태 | 설명 |
|------|------|------|
| Vue.js 프론트엔드 기본 구조 | ✅ 완료 | 대시보드, 회의 목록, 상세, 새 회의 |
| 실시간 마이크 녹음 | ✅ 완료 | MediaRecorder API (WebM) |
| 오디오 파일 업로드 | ✅ 완료 | 드래그 앤 드롭, 프로그레스 바 |
| Whisper STT 연동 | ✅ 완료 | 대용량 파일 자동 분할 포함 |
| Dify AI 요약 연동 | ✅ 완료 | TL;DR, 액션 아이템, 키워드 |
| 회의 검색 기능 | 🔲 예정 | 전문 검색 구현 |
| 화자 구분 | 🔲 예정 | Speaker Diarization 기술 검토 |
| 회의실 예약 | 🔲 예정 | 예약 CRUD 및 UI |

### Phase 2 — 기능 고도화

> **목표**: 운영 환경 전환을 위한 기능 완성 및 안정화

| 항목 | 설명 |
|------|------|
| 데이터베이스 도입 | PostgreSQL 또는 MongoDB 연동, Mock 데이터 제거 |
| 사용자 인증 | SSO / LDAP 연동을 통한 사내 계정 인증 |
| 회의실 예약 시스템 | 달력/타임라인 뷰, 충돌 방지, 회의록 자동 연결 |
| 화자 구분 고도화 | Diarization 모델 적용 및 수동 매핑 UI |
| 전문 검색 엔진 | Elasticsearch 또는 DB 전문 검색 인덱스 적용 |
| 스플릿 뷰 고도화 | 오디오-텍스트 동기화, 인라인 편집 |
| 내보내기 기능 | PDF, Markdown, 클립보드 복사 |

### Phase 3 — 사내망 이관 및 운영

> **목표**: 프라이빗 클라우드 또는 사내망 배포, 전사 서비스 오픈

| 항목 | 설명 |
|------|------|
| 인프라 구성 | 프라이빗 클라우드 / 사내 서버 배포 |
| CI/CD 파이프라인 | 자동 빌드, 테스트, 배포 파이프라인 구축 |
| 모니터링 | 서비스 헬스 체크, 에러 트래킹, 사용량 대시보드 |
| 보안 강화 | HTTPS, 데이터 암호화, 접근 제어 정책 |
| 사용자 교육 | 사내 매뉴얼 배포 및 교육 세션 |

### Phase 4 — 확장 (장기)

> **목표**: 고도화 기능 및 외부 시스템 연동

| 항목 | 설명 |
|------|------|
| 화상회의 연동 | Zoom, Microsoft Teams 녹음 자동 가져오기 |
| 사내 메신저 연동 | 액션 아이템 알림, 회의 요약 자동 공유 |
| 캘린더 동기화 | Google Calendar, Outlook 연동 |
| 다국어 지원 | 영어, 일본어 등 다국어 STT 및 요약 |
| 모바일 대응 | 반응형 웹 또는 PWA |
| AI 고도화 | 회의 패턴 분석, 자동 안건 생성, 의사결정 지원 |

---

## 부록

### A. 용어 정의

| 용어 | 설명 |
|------|------|
| STT | Speech-to-Text, 음성을 텍스트로 변환하는 기술 |
| Diarization | 오디오에서 화자를 구분하는 기술 (Speaker Diarization) |
| Chunking | 대용량 파일을 일정 크기 단위로 분할하는 처리 |
| TL;DR | "Too Long; Didn't Read"의 약자, 핵심 요약 |
| Dify | LLM 기반 워크플로우 자동화 플랫폼 |
| AX | AI Transformation, AI 기반 업무 혁신 |

### B. 환경 변수

| 변수명 | 설명 | 필수 여부 |
|--------|------|-----------|
| `OPENAI_API_KEY` | OpenAI API 인증 키 (Whisper STT용) | 필수 |
| `DIFY_API_KEY` | Dify Workflow API 인증 키 | 필수 |
| `DIFY_API_URL` | Dify API 엔드포인트 URL | 필수 |

---

> **문서 이력**
>
> | 버전 | 일자 | 변경 내용 | 작성자 |
> |------|------|-----------|--------|
> | v1.0 | 2026-03-22 | 최초 작성 | AX 혁신 TF |
> | v1.1 | 2026-03-22 | 회의실 예약(사용자 스토리, 화면설계, 반복예약, 알림) 및 회의 검색(글로벌 검색바, 고급 검색, 가중치, 구현 전략) 기능 대폭 강화 | AX 혁신 TF |
